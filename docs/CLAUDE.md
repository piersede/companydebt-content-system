This directory is for system documentation.

Rules:

- document the default runtime path, not just the canonical archive
- keep quality supremacy explicit
- when token optimization changes workflow, update docs so humans are not asked to choose context manually
