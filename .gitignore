# Python
__pycache__/
*.pyc
create_spreadsheet.py

# OS
.DS_Store
Thumbs.db
desktop.ini

# Sensitive — never commit these
.env
.env.local
*.env
.env.bak-*
credentials*
secrets*

# Bernstein runtime state
editorial-os/bernstein-state/

# Build artifacts — generated HTML, PHP, dumps, batch outputs
tmp/
case_studies_hub_raw.html
content_cache.json
dconduct_pages.json
_page_*.html

# Claude Code worktrees
.claude/worktrees/

# Outreach conductor runtime state + secrets
outreach/state/
outreach/outbox/
outreach/node_modules/
outreach/.graphcache.json

# 24MB extract; the 4.5MB source zip is committed instead
data/insolvency-statistics/record-level/record-level-data.csv
