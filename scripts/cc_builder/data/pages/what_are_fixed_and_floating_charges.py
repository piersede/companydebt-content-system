"""Page config for: Fixed and Floating Charges Explained for UK Company Directors

Live at /advice/what-are-fixed-and-floating-charges/ (WP pages 24760).

Adopted into Bernstein by registration only -- no content was regenerated.
Page class ("what-are-fixed-and-floating-charges" -> "entity_owner") sits in
scripts/page_runtime_metadata.py SLUG_PAGE_CLASS_OVERRIDES.

verification_date is the date the draft was last changed in git, not a claim
that the page has been fact-checked since. It exists so the runtime router can
infer an honest freshness tier.

Slim config -- only the fields the Bernstein pipeline and runtime-pack router
read. Content is read directly from drafts/24760_what-are-fixed-and-floating-charges.html.
"""

PAGE_CONFIG = {
    'slug': "what-are-fixed-and-floating-charges",
    'page_type': "definition",
    'wp_page_id': 24760,
    'title': "Fixed and Floating Charges Explained for UK Company Directors",
    'verification_date': "16 July 2026",
}
