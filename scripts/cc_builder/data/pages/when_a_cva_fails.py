"""Page config for: What Happens if a Company Voluntary Arrangement (CVA) Fails?

Live at /company-rescue-solutions/company-voluntary-arrangement/when-a-cva-fails/ (WP pages 74390).

Adopted into Bernstein by registration only -- no content was regenerated.
Page class ("when-a-cva-fails" -> "recovery_strategy") sits in
scripts/page_runtime_metadata.py SLUG_PAGE_CLASS_OVERRIDES.

verification_date is the date the draft was last changed in git, not a claim
that the page has been fact-checked since. It exists so the runtime router can
infer an honest freshness tier.

Slim config -- only the fields the Bernstein pipeline and runtime-pack router
read. Content is read directly from drafts/74390_when-a-cva-fails.html.
"""

PAGE_CONFIG = {
    'slug': "when-a-cva-fails",
    'page_type': "process_guide",
    'wp_page_id': 74390,
    'title': "What Happens if a Company Voluntary Arrangement (CVA) Fails?",
    'verification_date': "14 July 2026",
}
