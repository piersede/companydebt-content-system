This subtree owns the credit-card page build system.

When working here:

- preserve build correctness and quality gates
- keep runtime-pack routing visible for article builds
- do not weaken quality checks to make builds easier
- prefer exposing context decisions rather than asking humans to choose packs manually
